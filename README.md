# Classic Fireplace

Client repository for Classic Fireplace projects.

## Active project branches

- `cf-serviceops` - ServiceOps application for intake, customer matching, schedule visibility, Apps Script operator webapp, and Next.js assistant/streaming backend.

Keep project-specific application files on their own branches so `main` can stay a lightweight client index.
