# R3.4.21c — Requested By Organizer packaging fix

Date: 2026-09-11

Scope: deployment tooling only; PreInspection business change remains Requested By = Calendar organizer resolved to exact Striven Employee.

## R3.4.21b observed result

The package completed authorization, self-test, PRE pull, two-file patch, syntax checks, and FRESH pull. It then stopped before any production push because PowerShell StrictMode raised an error when the manifest-difference function returned zero objects and the wrapper attempted to read `.Count` from `$null`.

No production Apps Script write occurred in R3.4.21b.

## R3.4.21c correction

Manifest-difference results are explicitly normalized to arrays before count checks. The changed-file result is likewise normalized to an array. Existing isolated clasp project files, exact Script ID pinning, allowlisted two-file patching, syntax checks, freshness guard, POST full-project SHA verification, and automatic rollback remain unchanged.

Status at the time of this note: package rebuilt; deployment was not yet claimed until a run ended with `DEPLOYED_SOURCE_VERIFIED` and POST verification passed.

Later release records supersede this status; see `executions/latest.json` for current production truth.
